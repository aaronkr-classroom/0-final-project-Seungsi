<!-- views/space/edit -->

<!--
  Listing 20.1 (p. 290)
  사용자 입력 폼 예제
-->

<main class="space-profile">
  <div class="container col-xl-10 col-xxl-8 px-4 py-5">
    <div class="pt-5">
      <h1>
        space Data for <%= space.name %>
        <span class="btn btn-outline-danger">Editing</span>
      </h1>
      <div class="row">
          <div class="col col-lg-2" style="position: relative">
            <figure class="space-img mt-5 mb-3">
              <img src="<%= space.profileImg || "https://via.placeholder.com/150"
              %>" alt="Placeholder image" class="rounded-circle" />
            </figure>
            <span
              class="btn profileImg-edit-btn btn-outline-danger"
              id="profileImg-edit-button"
              >✏️</span
            >
            <input
              type="text"
              name="profileImg"
              id="inputProfileImg"
              class="form-control profileImg-edit-input"
              value="<%= space.profileImg %>"
              placeholder="URL for profile image"
            />
          </div>
          <div class="col col-lg-10">
              <!-- Listing 20.4 (p. 292) -->
              <!-- 사용자 데이터 update로의 폼 추가 -->
              <form
                class="sports-edit-form mb-3"
                method="POST"
                action="<%= `/space/${space._id}/update?_method=PUT` %>"
              >
                <table class="table table-striped table-hover mt-5 mb-5">
                  <!-- 사용자 데이터를 보여주기 위한 테이블 추가 -->
                  <tbody>
                    <tr>
                      <th scope="col">name</th>
                      <td colspan="2" class="pt-0 pb-0 px-0">
                        <input
                          type="text"
                          name="name"
                          id="inputname"
                          class="form-control"
                          value="<%= space.name %>"
                          placeholder="name"
                          autofocus
                        />
                      </td>
                    </tr>
                    <tr>
                      <th scope="col">Email</th>
                      <td colspan="2" class="pt-0 pb-0 px-0">
                        <input
                          type="email"
                          name="email"
                          id="inputEmail"
                          class="form-control"
                          value="<%= space.email %>"
                          placeholder="Email address"
                          required
                        />
                      </td>
                    </tr>
                    <tr>
                      <th scope="col">전화번호</th>
                      <td colspan="2" class="pt-0 pb-0 px-0">
                        <input
                          type="phoneNumber"
                          name="phoneNumber"
                          id="inputphoneNumber"
                          class="form-control"
                          value="<%= space.phoneNumber %>"
                          placeholder="phoneNumber"
                          required
                        />
                      </td>
                    </tr>
                    <tr>
                      <th scope="col">Password (+ Confirm)</th>
                      <td class="pt-0 pb-0 px-0">
                        <input
                          type="password"
                          name="password"
                          id="inputPassword"
                          class="form-control"
                          value="<%= space.password %>"
                          placeholder="Password"
                        />
                      </td>
                      <td class="pt-0 pb-0 px-0">
                        <input
                          type="password"
                          name="password2"
                          id="inputPassword2"
                          class="form-control"
                          value=""
                          placeholder="Confirm password"
                        />
                      </td>
                    </tr>
                    <tr>
                      <th scope="col">Subscribed Account</th>
                      <td colspan="2">
                        <label>
                          <input type="checkbox" value="subscribe" checked />
                          Subscribed to Newsletter
                        </label>
                      </td>
                    </tr>
                    <tr>
                      <th scope="col">Created</th>
                      <td colspan="2"><%= space.createdAt %></td>
                    </tr>
                    <tr>
                      <th scope="col">Updated</th>
                      <td colspan="2"><%= space.updatedAt %></td>
                    </tr>
                    <tr>
                      <th scope="col">ID</th>
                      <td colspan="2"><%= space._id %></td>
                    </tr>
                  </tbody>
              </table>
              <button type="submit" class="btn btn-info">💾 Update Sports</button>
              <a href="/space/<%= space._id %>" class="btn btn-danger"
                >❌ Cancel</a
              >
            </form>
          </div>
        </div>
        <% if (space.subscribedAccount) { %>
          <h4 class="mt-5 text-center">
            This space has a subscribed account:
            <a href="/space/<%= space.subscribedAccount %>">
              <%= space.subscribedAccount.name %> </a
            >.
          </h4>
          <% } %>
          <div class="row">
              <div class="col col-lg-10 offset-lg-2">
                <!-- 프로필 페이지에서 사용자 계정의 편집 및 삭제를 위한 링크 추가 -->
                <a href="/space" class="btn btn-primary">← Back to Sports</a>
                <a
                  href="/space/<%= space._id %>/delete?_method=DELETE"
                  onclick="return confirm('Are you sure you want to delete this record?')"
                  class="btn btn-danger"
                  role="button"
                  >🗑️ Delete Space</a
                >
              </div>
            </div>
          </div>
        </div>
      </main>