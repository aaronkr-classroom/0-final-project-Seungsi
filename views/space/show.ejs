<!-- views/space/show.ejs -->

<!--
  Listing 19.5 (p. 283)
  사용자 Show 테이블
-->
<main class="space">
  <div class="container col-xl-10 col-xxl-8 px-4 py-5">
    <div class="pt-5">
      <h1>User Data for <%= space.name %></h1>

      <div class="row">
        <div class="col col-lg-2">
          <figure class="user-img mt-5 mb-5">
            <img src="<%= space.profileImg || "https://via.placeholder.com/150"
            %>" alt="Placeholder image" class="rounded-circle profile-img large-img" />
          </figure>
        </div>
        <div class="col col-lg-10">
          <table class="table table-striped table-hover mt-5 mb-5">
            <!-- 사용자 데이터를 보여주기 위한 테이블 추가 -->
            <tbody>
              <tr>
                <th scope="col">name</th>
                <td><%= space.name %></td>
              </tr>
              
              <tr>
                <th scope="col">Email</th>
                <td><%= space.email %></td>
              </tr>
              
              <tr>
                  <th scope="col">Phonenumber</th>
                  <td><%= space.phoneNumber %></td>
                </tr>

              <tr>
                <th scope="col">Password</th>
                <td><%= space.password %></td>
              </tr>
              <tr>
                <th scope="col">Subscribed Account</th>
                <td><%= space.subscribedAccount %></td>
              </tr>
              <tr>
                <th scope="col">Created</th>
                <td><%= space.createdAt %></td>
              </tr>
              <tr>
                <th scope="col">Updated</th>
                <td><%= space.updatedAt %></td>
              </tr>
              <tr>
                <th scope="col">ID</th>
                <td><%= space._id %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
      <div class="row">
          <div class="col col-lg-10 offset-lg-2">
            <!-- 프로필 페이지에서 사용자 계정의 편집 및 삭제를 위한 링크 추가 -->
            <a href="/space" class="btn btn-primary">← Back to sports</a>
            <a
              href="/space/<%= space._id %>/edit"
              class="btn btn-secondary"
              role="button"
              >✏️ Edit space</a
            >
            <a
              href="/space/<%= space._id %>/delete?_method=DELETE"
              onclick="return confirm('Are you sure you want to delete this record?')"
              class="btn btn-danger disabled"
              role="button"
              aria-disabled="true"
              >🗑️ Delete space</a
            >
          </div>
        </div>
      </div>
    </div>
  </main>